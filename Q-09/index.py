#Calculate Are of triangle
height = float(input('Enter the height of the triangle:'))
base = float(input('Enter teh base of the triangle:'))
area = (0.5)*base*height
print('The area of triangle is' , area)
